Install from the existing Replit workspace root:

  unzip -o Giga-Cat-Bottom-Stats-Patch-v21.zip
  bash Giga-Cat-Bottom-Stats-Patch-v21/install.sh

Then push your repo normally.
No automatic server/process restart is performed.
